# ETF Horizon runs with NO API keys or environment variables required!
# Market data is retrieved server-side inside the MCP server from Yahoo Finance's public chart endpoint.
# Ready to deploy on Vercel from GitHub with zero configuration.

PORT=3000
